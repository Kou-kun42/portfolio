<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1, shrink-to-fit=no"
        />
        <title>Login</title>
        <!-- Favicon-->
        <link rel="icon" href="/static/img/favicon.png" />
        <!-- Google fonts-->
        <link href="/static/styles/googlefont.css" rel="stylesheet" />
        <link href="/static/styles/googlefont2.css" rel="stylesheet" />
        <!-- Bootstrap -->
        <link href="/static/styles/bootstrap.css" rel="stylesheet" />
        <!-- Custom Styles -->
        <link href="/static/styles.css" rel="stylesheet" />
    </head>
    <body>
        <div class="d-flex justify-content-center">
            <form class="col-lg-3" action="/signin" method="POST">
                <h1 class="my-4">sign in</h1>
                <div class="row justify-content-center">
                    <div class="form-group">
                        <label for="username">username</label>
                        <input
                            required
                            type="text"
                            name="username"
                            id="username"
                            class="form-control"
                        />
                    </div>
                    <div class="form-group">
                        <label for="password">password</label>
                        <input
                            required
                            type="password"
                            name="password"
                            id="password"
                            class="form-control"
                        />
                    </div>
                    <button type="submit" class="my-4 btn btn-primary">
                        submit
                    </button>
                </div>
            </form>
        </div>
    </body>
</html>
